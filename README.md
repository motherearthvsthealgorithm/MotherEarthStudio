# Mother Earth Studio v0.11.3

Adaptive Typography Engine release. Story Highlights are now the primary overlay source, while SRT remains a supported fallback. Install over the current Mother Earth Studio folder; the installer backs up application code and preserves creator data.
